#include<stdio.h>            
int main(void)                   //这行代码由三个部分组成，分别是返回类型int，表明返回类型为整数。函数名main()，main()是主函数，一个文件中只能有一个主函数。括号内为传入函数的信息，此处为void。在括号内最好不要留空格，因为C99和C11不允许这么写。
{
     int  students;              //此处是声明变量，所有的变量必须先声明才能使用。
     students = 5;               //此处是赋值变量

     printf("There are %d students in class."
     ,students);                 //双引号""内的是这个函数的参数，参数有两种，分别是实际参数和形式参数。这里的参数是实际参数。%d是一个占位符，作用是指明输出变量的位置。
   return 0;                     //第二行的int要求main()函数应该返回一个整数。C标准要求有返回值的C函数应加上return语句。可以遗漏，但最好不要。
}
                                 //第二行为函数头，第3行到第9行为函数块。
